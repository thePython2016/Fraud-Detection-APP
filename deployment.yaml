apiVersion: apps/v1
kind: Deployment
metadata:
  name: fraud-prediction-model
spec:
  replicas: 2
  selector:
    matchLabels:
      app: fraud-prediction-model
  template:
    metadata:
      labels:
        app: spam-prediction-modeltext
    spec:
      containers:
      - name: fraud-prediction
        image: fraud-prediction-model
        ports:
        - containerPort: 8501
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"